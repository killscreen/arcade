/*global define*/


// This is an example of Scene API
define(
    function () {
        
        function Scene(context, message) {
            return {
                update();
            };
        }

        return Scene;
    }
);
